# packages/skills

Future home for canonical skill loader, validator, registry, composer, renderer, and auditor.

Current implementation lives in `src/lib/agent-skills` until workspace migration is complete.
