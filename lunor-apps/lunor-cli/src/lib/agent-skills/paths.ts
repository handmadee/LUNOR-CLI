import { join } from 'node:path';

export const AGENTS_DIR = '.agents';
export const AGENT_SKILLS_DIR = join(AGENTS_DIR, 'skills');
export const AGENT_TARGETS_DIR = join(AGENTS_DIR, 'targets');
export const AGENT_REGISTRY_DIR = join(AGENTS_DIR, 'registry');
